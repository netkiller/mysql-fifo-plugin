cmake_minimum_required(VERSION 2.8)
PROJECT(fifo)
ADD_LIBRARY(fifo SHARED fifo.c)
INCLUDE_DIRECTORIES(/usr/include/mysql)
#TARGET_LINK_LIBRARIES(fifo )
INSTALL(PROGRAMS libfifo.so DESTINATION /usr/lib64/mysql/plugin/)
